pull_request_rules:
  - name: vm1 dismiss approvals only synchronize
    conditions:
      - title = VM1_DISMISS_SYNC_NONPUSH
    actions:
      dismiss_reviews:
        approved: true
        changes_requested: false
        when: synchronize
