merge_protections:
  - name: vm1-duplicate-mp-name
    if: true
    success_conditions:
      - label = bug
  - name: vm1-duplicate-mp-name
    if: true
    success_conditions:
      - label = enhancement
