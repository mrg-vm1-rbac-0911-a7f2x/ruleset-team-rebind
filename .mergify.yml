pull_request_rules:
  - name: VM1 bot-account branch credential probe
    conditions:
      - title ~= ^VM1_BOT_CRED_
    actions:
      merge:
        method: merge
        merge_bot_account: livastic-vm1-secondary
